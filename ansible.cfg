[defaults]
inventory = inventory
remote_user = ansible
private_key_file = ~/.ssh/id_rsa
host_key_checking = false

[privilege_escalation]
become=true
become_method=sudo
become_user=root
become_ask_pass=False
